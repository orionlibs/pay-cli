spring:
  application:
    name: yapily-paycli
  main:
    web-application-type: none
  shell:
    enabled: true
    interactive:
      enabled: true
    noninteractive:
      enabled: false
    history:
      enabled: true
    command:
      version:
        enabled: true

#logging:
#  level:
#    root: INFO
#    org.springframework.shell: DEBUG
#    platform.application: DEBUG